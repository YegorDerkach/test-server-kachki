# Налаштування змінних оточення (.env)

## 📁 Файли

- `.env` - ваш локальний файл з чутливими даними (НЕ комітиться в git)
- `.env.example` - шаблон для інших розробників (комітиться в git)

## 🚀 Використання

### Варіант 1: Експорт змінних вручну (Mac/Linux)

```bash
# Завантажити змінні з .env файлу
export $(cat .env | xargs)

# Запустити додаток
cd web-kachki-server/server
./mvnw spring-boot:run
```

### Варіант 2: Використання з shell скриптом

Створіть файл `load-env.sh`:

```bash
#!/bin/bash
export $(cat .env | grep -v '^#' | xargs)
cd web-kachki-server/server
./mvnw spring-boot:run
```

Запуск:
```bash
chmod +x load-env.sh
./load-env.sh
```

### Варіант 3: Використання з IDE

Більшість IDE (IntelliJ IDEA, VS Code) автоматично завантажують `.env` файл, якщо встановлено відповідний плагін.

**IntelliJ IDEA:**
- Встановіть плагін "EnvFile"
- Або вручну налаштуйте Run Configuration → Environment variables → Load from file

**VS Code:**
- Встановіть розширення "DotENV"

### Варіант 4: Використання з Maven

Створіть файл `web-kachki-server/server/.mvn/jvm.config`:

```
-Dserver.port=${SERVER_PORT}
-Dspring.data.mongodb.uri=${MONGODB_URI}
-Djwt.secret=${JWT_SECRET}
-Dgcp.credentials.location=${GCP_CREDENTIALS_LOCATION}
```

Але краще використовувати системні змінні оточення.

## 🔧 Налаштування

1. Скопіюйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Відредагуйте `.env` файл і встановіть ваші значення:
   ```bash
   nano .env
   # або
   code .env
   ```

3. Особливо важливо налаштувати:
   - `MONGODB_URI` - ваш MongoDB connection string
   - `JWT_SECRET` - використовуйте сильний випадковий ключ
   - `GCP_CREDENTIALS_LOCATION` - шлях до вашого GCP credentials файлу

## ⚠️ Безпека

- **НЕ комітьте** `.env` файл в git!
- `.env` вже додано в `.gitignore`
- Використовуйте `.env.example` як шаблон для команди
- Для продакшену використовуйте змінні оточення на сервері (Render.com, тощо)

## 🔄 Оновлення .env.example

Якщо додали нову змінну:
1. Додайте її в `.env.example` (без реальних значень)
2. Додайте в `.env` з вашими значеннями
3. Закомітьте `.env.example` (але не `.env`!)

## 📝 Приклад використання

```bash
# 1. Завантажити змінні
export $(cat .env | grep -v '^#' | xargs)

# 2. Перевірити, що змінні завантажені
echo $MONGODB_URI
echo $JWT_SECRET

# 3. Запустити додаток
cd web-kachki-server/server
./mvnw spring-boot:run
```

## 🐳 Docker

Для Docker використовуйте `--env-file`:

```bash
docker run --env-file .env your-image
```

## 🌐 Render.com / Production

На Render.com або інших хостингах використовуйте Environment Variables в панелі управління, а не `.env` файл.

