spring.application.name=web-kachki-server

# Server port (Render uses PORT, fallback to SERVER_PORT or 8080)
server.port=${PORT:${SERVER_PORT:8080}}

# MongoDB connection (use environment variable for production)
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://guzzzlik:pisunf6@database.lkulppx.mongodb.net/server?retryWrites=true&w=majority&appName=database}

# JWT Secret (use environment variable for production)
jwt.secret=${JWT_SECRET:nasamom-dele-dele-dele-ya-devushka-naruto}

# Google Cloud Storage configuration
gcs.bucket.video.name=${GCS_BUCKET_VIDEO:video-kachki}
gcs.bucket.photo.name=${GCS_BUCKET_PHOTO:photo-kachki}
gcp.project-id=${GCP_PROJECT_ID:web-kachki}

# GCP Credentials (use environment variable GOOGLE_APPLICATION_CREDENTIALS or set path here)
# For production: set GOOGLE_APPLICATION_CREDENTIALS environment variable or gcp.credentials.location
gcp.credentials.location=${GCP_CREDENTIALS_LOCATION:}

# File upload limits
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB